[toolchain]

[features]
resolution = true
skip-lint = false

[programs.localnet]
rust_demo2 = "GvBrcFw27V61tUgGrvr9tnp1Ajbt3ETofsm949MkUBFR"


[registry]
url = "https://api.apr.dev"

[provider]
cluster = "Localnet"
# cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"


# [cluster.devnet]
# json_rpc_url = "https://api.devnet.solana.com"
# websocket_url = "wss://api.devnet.solana.com"


# [test]
# skip_local_validator = true # 不自动启动测试验证器
# skip_deploy = true          # 不重新部署程序
